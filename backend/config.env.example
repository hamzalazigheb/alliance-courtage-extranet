# Backend environment example

NODE_ENV=production
PORT=3000

# Database
DB_HOST=mysql
DB_PORT=3306
DB_USER=alliance
DB_PASSWORD=change_me_db
DB_NAME=alliance_courtage

# CORS (set to your EC2 public IP or domain for production)
CORS_ORIGIN=http://localhost,http://localhost:5173,http://localhost:5174,http://YOUR_EC2_PUBLIC_IP

# JWT secret
JWT_SECRET=change_me_jwt_secret

# Upload directory inside container
UPLOAD_DIR=/app/uploads


